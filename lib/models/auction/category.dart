import 'package:flutter/material.dart';

class Category {
  String name;
  String description;
  String iconName;

  Category(
      {@required this.name,
      @required this.description,
      @required this.iconName});
}
